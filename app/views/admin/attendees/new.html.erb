<h1>New attendee</h1>

<% form_for([:admin, @attendee]) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals => {:f => f} %>

  <p>
    <%= f.submit 'Create' %>
  </p>

<% end %>

<%= link_to 'Back', admin_attendees_path %>
